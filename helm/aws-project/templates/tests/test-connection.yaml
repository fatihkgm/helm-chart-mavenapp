apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws-project.fullname" . }}-test-connection"
  labels:
{{ include "aws-project.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "aws-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
