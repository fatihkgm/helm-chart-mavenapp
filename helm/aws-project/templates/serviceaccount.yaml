{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "aws-project.serviceAccountName" . }}
  labels:
{{ include "aws-project.labels" . | indent 4 }}
{{- end -}}
